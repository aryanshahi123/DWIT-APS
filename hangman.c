#include <stdio.h>

char displayer(char chosen)
{
    for (i = 0; chosen[i] != '\0'; i++)
    {
    }
}

void main()
{
    char word[20] = "COMPUTER";
    char chosen[20], already[20], letter;
    int count = 0;

    while (count < 5)
    {
        printf("Enter a letter:");
        scanf(" %c", &letter);
    }
}